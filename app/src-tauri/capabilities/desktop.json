{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Capabilities scoped to desktop platforms only — used by plugins that are gated behind #[cfg(desktop)] in lib.rs (window-state).",
  "platforms": ["windows"],
  "windows": ["main", "notademics-*"],
  "permissions": ["window-state:default"]
}
